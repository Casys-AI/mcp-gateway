{
  "name": "Casys MCP Sandbox",
  "dockerFile": "Dockerfile",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-toolsai.jupyter",
        "denoland.vscode-deno"
      ],
      "settings": {
        "deno.enable": true,
        "jupyter.notebookFileRoot": "${workspaceFolder}"
      }
    }
  }
}
